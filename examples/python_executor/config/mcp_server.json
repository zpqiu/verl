{
    "mcpServers": {
        "Python Executor": {
            "command": "python", 
            "args": ["-m", "verl.tools.mcp_services.python_executor_server", "--host", "127.0.0.1", "--port", "8001"],
            "env": {
                "PYTHONPATH": "."
            }
        }
    }
}